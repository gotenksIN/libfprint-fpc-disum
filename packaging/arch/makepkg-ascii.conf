#!/usr/bin/env bash

source /etc/makepkg.conf

# umockdev cannot replay USB captures when their absolute path contains
# non-ASCII characters. Keep makepkg's source and build trees under /tmp.
BUILDDIR="/tmp/libfprint-build-${USER}"
